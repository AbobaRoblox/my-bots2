# 🤖 Python Боты с Автозапуском и Хостингом

Улучшенная система для запуска двух Python ботов (`bot.py` и `shopbot.py`) с поддержкой:
- ✅ Автозапуск при старте Windows (локально)
- ✅ Запуск на хостинге через Docker (удаленно)
- ✅ Логирование всех действий
- ✅ Обработка ошибок и автоматический перезапуск
- ✅ Конфигурационные файлы

---

## 📋 Содержание

- [Файлы проекта](#файлы-проекта)
- [Запуск на Windows (локально)](#запуск-на-windows-локально)
- [Запуск на хостинге (Docker)](#запуск-на-хостинге-docker)
- [Конфигурация](#конфигурация)
- [Логи](#логи)
- [Улучшения в скриптах](#улучшения-в-скриптах)

---

## 📁 Файлы проекта

### Боты
- `bot.py` - основной бот с логированием и обработкой ошибок
- `shopbot.py` - бот для работы с магазином

### Автозапуск Windows
- `start_bot.vbs` - VBS скрипт для скрытого запуска bot.py
- `start_shopbot.vbs` - VBS скрипт для скрытого запуска shopbot.py
- `add_to_startup.bat` - добавить в автозагрузку Windows
- `remove_from_startup.bat` - удалить из автозагрузки Windows

### Хостинг (Docker)
- `Dockerfile` - образ Docker для обоих ботов
- `docker-compose.yml` - конфигурация Docker Compose
- `supervisord.conf` - конфигурация Supervisor (управление процессами)
- `start_hosting.sh` / `start_hosting.bat` - запуск на хостинге
- `stop_hosting.sh` / `stop_hosting.bat` - остановка на хостинге

### Конфигурация
- `config.json` - конфигурация для bot.py (создается автоматически)
- `shopbot_config.json` - конфигурация для shopbot.py (создается автоматически)
- `requirements.txt` - Python зависимости

---

## 🖥️ Запуск на Windows (локально)

### Установка

1. **Убедитесь, что Python установлен**
   ```bash
   python --version
   ```

2. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

3. **Добавьте в автозагрузку**
   - Запустите `add_to_startup.bat` **от имени администратора**
   - Правой кнопкой мыши → "Запуск от имени администратора"

4. **Готово!** Оба бота будут запускаться при каждом входе в систему

### Удаление из автозагрузки

Запустите `remove_from_startup.bat` для удаления ботов из автозагрузки.

### Ручной запуск

```bash
# Запуск bot.py
python bot.py

# Запуск shopbot.py
python shopbot.py
```

---

## 🌐 Запуск на хостинге (Docker)

### Требования

- Docker
- Docker Compose

### Быстрый старт

#### На Linux/Mac:
```bash
# Сделать скрипт исполняемым
chmod +x start_hosting.sh

# Запустить
./start_hosting.sh
```

#### На Windows:
```bash
# Просто запустите
start_hosting.bat
```

### Что происходит при запуске?

1. ✅ Создаются конфигурационные файлы (если их нет)
2. ✅ Собирается Docker образ с обоими ботами
3. ✅ Запускается контейнер с Supervisor
4. ✅ Supervisor управляет обоими ботами и автоматически перезапускает их при сбоях

### Управление контейнером

```bash
# Просмотр логов в реальном времени
docker-compose logs -f

# Просмотр логов только одного бота
docker-compose logs -f bots

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Или используйте скрипт
./stop_hosting.sh  # Linux/Mac
stop_hosting.bat   # Windows

# Статус контейнера
docker-compose ps

# Вход в контейнер для отладки
docker-compose exec bots bash
```

### Запуск на VPS/Сервере

1. **Загрузите проект на сервер**
   ```bash
   git clone <ваш-репозиторий>
   cd <папка-проекта>
   ```

2. **Отредактируйте конфигурации** (если нужно)
   ```bash
   nano config.json
   nano shopbot_config.json
   ```

3. **Запустите**
   ```bash
   chmod +x start_hosting.sh
   ./start_hosting.sh
   ```

4. **Настройте автозапуск при перезагрузке сервера**
   ```bash
   # Добавьте в crontab
   crontab -e
   
   # Добавьте строку:
   @reboot cd /path/to/project && docker-compose up -d
   ```

---

## ⚙️ Конфигурация

### config.json (для bot.py)

```json
{
    "check_interval": 60,
    "bot_name": "Bot",
    "debug": false
}
```

### shopbot_config.json (для shopbot.py)

```json
{
    "check_interval": 60,
    "bot_name": "ShopBot",
    "debug": false,
    "shop_url": "https://example.com",
    "max_retries": 3
}
```

### Параметры

- `check_interval` - интервал проверки в секундах
- `bot_name` - имя бота для логов
- `debug` - режим отладки (true/false)
- `shop_url` - URL магазина (только для shopbot)
- `max_retries` - количество попыток при ошибке (только для shopbot)

---

## 📊 Логи

### Локальный запуск (Windows)

Логи сохраняются в папке `logs/`:
- `bot_YYYYMMDD.log` - логи bot.py
- `shopbot_YYYYMMDD.log` - логи shopbot.py

### Хостинг (Docker)

Логи сохраняются в папке `logs/`:
- `bot_YYYYMMDD.log` - основные логи bot.py
- `bot_error.log` - ошибки bot.py
- `bot_output.log` - вывод bot.py
- `shopbot_YYYYMMDD.log` - основные логи shopbot.py
- `shopbot_error.log` - ошибки shopbot.py
- `shopbot_output.log` - вывод shopbot.py

### Просмотр логов

```bash
# В реальном времени
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Логи конкретного бота
tail -f logs/bot_*.log
tail -f logs/shopbot_*.log
```

---

## 🚀 Улучшения в скриптах

### Что добавлено:

1. **Логирование**
   - Все действия записываются в файлы
   - Ротация логов по дням
   - Разные уровни логирования (INFO, WARNING, ERROR, DEBUG)

2. **Обработка ошибок**
   - Перехват всех исключений
   - Graceful shutdown при остановке
   - Автоматические повторные попытки (для shopbot)

3. **Конфигурация**
   - JSON файлы для настроек
   - Автоматическое создание конфигов по умолчанию
   - Легко изменять параметры без редактирования кода

4. **Хостинг**
   - Docker контейнер для запуска на любом сервере
   - Supervisor для управления процессами
   - Автоматический перезапуск при сбоях
   - Простые скрипты для запуска/остановки

5. **Мониторинг**
   - Счетчик итераций
   - Временные метки в логах
   - Статус выполнения работы

---

## 🛠️ Разработка

### Добавление своего кода

В `bot.py` найдите функцию `do_work()`:
```python
def do_work():
    """Выполнение основной работы бота"""
    try:
        logger.info("Выполняется работа бота...")
        
        # ВАШ КОД ЗДЕСЬ
        
        return True
    except Exception as e:
        logger.error(f"Ошибка: {e}", exc_info=True)
        return False
```

То же самое для `shopbot.py` в функции `do_work(config)`.

### Добавление зависимостей

Добавьте в `requirements.txt`:
```txt
requests>=2.31.0
beautifulsoup4>=4.12.0
```

Затем пересоберите Docker образ:
```bash
docker-compose down
docker-compose build
docker-compose up -d
```

---

## 📝 Примечания

- Скрипты работают в бесконечном цикле
- При ошибках продолжают работать
- Можно запускать локально на Windows или на хостинге
- Все настраивается через конфигурационные файлы
- Supervisor автоматически перезапускает упавшие процессы
- Docker контейнер изолирован и не зависит от системы

---

## 🆘 Помощь

### Боты не запускаются на Windows

1. Проверьте, что Python в PATH: `python --version`
2. Проверьте логи в папке `logs/`
3. Запустите вручную: `python bot.py` или `python shopbot.py`

### Боты не запускаются в Docker

1. Проверьте логи: `docker-compose logs`
2. Проверьте статус: `docker-compose ps`
3. Пересоберите образ: `docker-compose build --no-cache`
4. Проверьте конфигурационные файлы

### Нужна помощь?

- Проверьте логи в папке `logs/`
- Включите режим отладки в конфигурации: `"debug": true`
- Запустите вручную для просмотра ошибок

---

## 📜 Лицензия

Свободное использование для личных и коммерческих целей.

