# 🐳 Полное руководство по Docker на Windows

## 📋 Содержание
1. [Что такое Docker](#что-такое-docker)
2. [Зачем нужен Docker для ботов](#зачем-нужен-docker-для-ботов)
3. [Установка Docker на Windows](#установка-docker-на-windows)
4. [Проверка установки](#проверка-установки)
5. [Запуск ботов в Docker](#запуск-ботов-в-docker)
6. [Решение проблем](#решение-проблем)

---

## 🤔 Что такое Docker?

**Docker** - это платформа для запуска приложений в изолированных контейнерах.

### Простыми словами:
Представьте, что Docker - это "виртуальная коробка", в которой работают ваши боты:
- 📦 Всё необходимое упаковано внутри
- 🔒 Изолировано от вашей системы
- 🚀 Легко перенести на любой сервер
- ♻️ Автоматически перезапускается при сбоях

### Аналогия:
**Без Docker:**
- Вы устанавливаете Python
- Устанавливаете все библиотеки
- Настраиваете окружение
- Если что-то сломается - сложно восстановить

**С Docker:**
- Всё уже настроено и упаковано
- Запускается одной командой
- Работает одинаково везде
- Легко перенести на хостинг

---

## 🎯 Зачем нужен Docker для ботов?

### Преимущества:

1. **Автоматический перезапуск**
   - Если бот упадет - Docker автоматически его перезапустит
   - Не нужно следить вручную

2. **Запуск на хостинге**
   - Можете легко перенести на любой VPS/сервер
   - Работает одинаково на Windows, Linux, Mac

3. **Изоляция**
   - Боты не влияют на вашу систему
   - Все логи в одном месте

4. **Два бота одновременно**
   - bot.py и shopbot.py работают параллельно
   - Управляются одной командой

5. **Supervisor**
   - Следит за процессами
   - Перезапускает при ошибках
   - Записывает логи

---

## 💻 Установка Docker на Windows

### Системные требования:

**Минимальные требования:**
- ✅ Windows 10 64-bit: Pro, Enterprise, или Education (Build 19041 или выше)
- ✅ Windows 11 64-bit: Любая версия
- ✅ 4GB RAM (рекомендуется 8GB)
- ✅ Включенная виртуализация в BIOS

**Важно:** Docker Desktop требует WSL 2 (Windows Subsystem for Linux 2)

---

## 📥 Пошаговая установка

### Шаг 1: Проверка версии Windows

1. Нажмите `Win + R`
2. Введите: `winver`
3. Нажмите Enter
4. Проверьте версию сборки (должна быть 19041 или выше)

---

### Шаг 2: Включение WSL 2

#### Вариант А: Через PowerShell (рекомендуется)

1. **Откройте PowerShell от имени администратора**
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (администратор)" или "Терминал (администратор)"

2. **Выполните команды:**

```powershell
# Включить WSL
wsl --install
```

Эта команда автоматически:
- ✅ Включит WSL
- ✅ Установит WSL 2
- ✅ Установит Ubuntu (по умолчанию)

3. **Перезагрузите компьютер**

4. **После перезагрузки** откройте PowerShell и выполните:

```powershell
# Проверить версию WSL
wsl --list --verbose

# Установить WSL 2 как версию по умолчанию
wsl --set-default-version 2
```

#### Вариант Б: Вручную (если первый не сработал)

1. **Откройте PowerShell от имени администратора**

2. **Включите компонент WSL:**
```powershell
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
```

3. **Включите компонент виртуализации:**
```powershell
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
```

4. **Перезагрузите компьютер**

5. **Скачайте и установите пакет обновления ядра Linux:**
   - Перейдите: https://aka.ms/wsl2kernel
   - Скачайте файл `wsl_update_x64.msi`
   - Установите его

6. **Установите WSL 2 по умолчанию:**
```powershell
wsl --set-default-version 2
```

---

### Шаг 3: Скачивание Docker Desktop

#### Способ 1: Через браузер

1. **Откройте сайт Docker:**
   ```
   https://www.docker.com/products/docker-desktop
   ```

2. **Нажмите кнопку "Download for Windows"**

3. **Скачается файл:** `Docker Desktop Installer.exe` (~500 MB)

#### Способ 2: Прямая ссылка

```
https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe
```

---

### Шаг 4: Установка Docker Desktop

1. **Запустите установщик** `Docker Desktop Installer.exe`

2. **В окне установки:**
   - ✅ Убедитесь, что включена опция **"Use WSL 2 instead of Hyper-V"**
   - ✅ Можно включить опцию **"Add shortcut to desktop"**

3. **Нажмите "Ok"** и дождитесь завершения установки

4. **После установки:**
   - Нажмите "Close and restart"
   - Компьютер перезагрузится

---

### Шаг 5: Первый запуск Docker Desktop

1. **После перезагрузки** запустите Docker Desktop:
   - Найдите в меню Пуск "Docker Desktop"
   - Или дважды кликните по ярлыку на рабочем столе

2. **Примите лицензионное соглашение:**
   - Прочитайте и нажмите "Accept"

3. **Войдите в аккаунт Docker (опционально):**
   - Можете создать бесплатный аккаунт
   - Или нажмите "Skip" для пропуска

4. **Дождитесь запуска Docker:**
   - В трее появится иконка Docker
   - Когда Docker запустится, иконка будет зеленой

---

## ✅ Проверка установки

### Шаг 1: Проверка через PowerShell

1. **Откройте PowerShell** (необязательно от имени администратора)

2. **Выполните команды:**

```powershell
# Проверить версию Docker
docker --version

# Должно вывести что-то вроде:
# Docker version 24.0.6, build ed223bc

# Проверить версию Docker Compose
docker-compose --version

# Должно вывести что-то вроде:
# Docker Compose version v2.21.0
```

### Шаг 2: Тестовый контейнер

```powershell
# Запустить тестовый контейнер
docker run hello-world
```

**Если всё работает, вы увидите:**
```
Hello from Docker!
This message shows that your installation appears to be working correctly.
```

---

## 🚀 Запуск ботов в Docker

Теперь, когда Docker установлен, можно запустить ботов!

### Вариант 1: Через скрипт (самый простой)

1. **Откройте папку с проектом** в Проводнике

2. **Дважды кликните на файл:**
   ```
   start_hosting.bat
   ```

3. **Готово!** Скрипт автоматически:
   - ✅ Создаст конфигурационные файлы
   - ✅ Соберет Docker образ
   - ✅ Запустит оба бота

### Вариант 2: Через PowerShell

1. **Откройте PowerShell**

2. **Перейдите в папку проекта:**
```powershell
cd "C:\гайд"
```

3. **Запустите:**
```powershell
docker-compose up -d
```

4. **Просмотр логов:**
```powershell
docker-compose logs -f
```

### Вариант 3: Через Docker Desktop (GUI)

1. **Откройте Docker Desktop**

2. **Перейдите в раздел "Containers"**

3. **Нажмите "+" → "From docker-compose.yml"**

4. **Выберите файл** `docker-compose.yml` из папки проекта

5. **Нажмите "Start"**

---

## 🎯 Управление ботами

### Основные команды:

```powershell
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Логи (в реальном времени)
docker-compose logs -f

# Логи (последние 100 строк)
docker-compose logs --tail=100

# Статус
docker-compose ps

# Войти в контейнер (для отладки)
docker-compose exec bots bash
```

### Или через скрипты:

```powershell
# Запуск
.\start_hosting.bat

# Остановка
.\stop_hosting.bat
```

---

## 📊 Просмотр логов

### В PowerShell:

```powershell
# Все логи
docker-compose logs

# Следить за логами в реальном времени
docker-compose logs -f

# Только ошибки
docker-compose logs | Select-String "ERROR"
```

### В папке logs/:

Откройте папку `logs/` в Проводнике:
- `bot_YYYYMMDD.log` - логи bot.py
- `shopbot_YYYYMMDD.log` - логи shopbot.py
- `bot_error.log` - ошибки bot.py
- `shopbot_error.log` - ошибки shopbot.py

---

## 🔧 Решение проблем

### Проблема 1: "WSL 2 installation is incomplete"

**Решение:**
```powershell
# Обновите WSL
wsl --update

# Перезапустите Docker Desktop
```

### Проблема 2: "Docker Desktop requires Windows 10 Pro or Enterprise"

**Решение для Windows 10 Home:**
- Обновите Windows до последней версии
- WSL 2 работает на Windows 10 Home начиная с версии 2004

### Проблема 3: Виртуализация не включена

**Решение:**
1. Перезагрузите компьютер
2. Войдите в BIOS (обычно F2, F10, или Del при загрузке)
3. Найдите настройку "Virtualization Technology" или "Intel VT-x" / "AMD-V"
4. Включите её
5. Сохраните и выйдите из BIOS

### Проблема 4: "docker-compose: command not found"

**Решение:**
- Убедитесь, что Docker Desktop запущен
- Перезапустите PowerShell
- Или используйте: `docker compose` (без дефиса)

### Проблема 5: Медленная работа Docker

**Решение:**
1. Откройте Docker Desktop
2. Настройки (⚙️) → Resources
3. Увеличьте CPU и Memory
4. Нажмите "Apply & Restart"

### Проблема 6: Контейнер не запускается

**Решение:**
```powershell
# Посмотрите логи ошибок
docker-compose logs

# Пересоберите образ
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

## 📖 Дополнительные ресурсы

### Официальная документация:
- Docker Desktop: https://docs.docker.com/desktop/windows/install/
- WSL 2: https://docs.microsoft.com/en-us/windows/wsl/install

### Видео-уроки:
- Официальный канал Docker: https://www.youtube.com/c/DockerIo
- Docker для начинающих (русский): поиск на YouTube

---

## 🎯 Краткая шпаргалка

```powershell
# УСТАНОВКА
wsl --install                    # Установить WSL 2
# Скачать Docker Desktop с docker.com
# Установить и перезагрузить

# ПРОВЕРКА
docker --version                 # Проверить Docker
docker-compose --version         # Проверить Compose

# ЗАПУСК БОТОВ
cd "C:\гайд"                    # Перейти в папку
.\start_hosting.bat             # Запустить (или)
docker-compose up -d            # Запустить вручную

# УПРАВЛЕНИЕ
docker-compose ps               # Статус
docker-compose logs -f          # Логи
docker-compose restart          # Перезапуск
docker-compose down             # Остановка

# ОТЛАДКА
docker-compose logs --tail=100  # Последние логи
docker-compose exec bots bash   # Войти в контейнер
```

---

## ✨ Итого

После установки Docker вы сможете:

1. ✅ Запускать оба бота одновременно
2. ✅ Автоматический перезапуск при сбоях
3. ✅ Легко перенести на любой хостинг
4. ✅ Изолированное окружение
5. ✅ Удобное управление логами

**Время установки:** ~20-30 минут  
**Сложность:** Средняя (следуйте инструкциям)  
**Результат:** Профессиональное решение для запуска ботов 🚀

---

## 🆘 Нужна помощь?

Если возникли проблемы:
1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Убедитесь, что Docker Desktop запущен (зеленая иконка в трее)
3. Попробуйте перезагрузить компьютер
4. Проверьте логи: `docker-compose logs`

---

**Готово! Теперь вы знаете всё о Docker на Windows!** 🎉

